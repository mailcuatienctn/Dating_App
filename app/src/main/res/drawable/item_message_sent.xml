<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Tin nhắn gửi đi -->
    <TextView
        android:id="@+id/text_message_body_sent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_message_sent"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="8dp"
        android:elevation="1dp"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:maxWidth="280dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintHorizontal_bias="1.0" />

    <!-- Thời gian gửi tin nhắn -->
    <TextView
        android:id="@+id/text_message_time_sent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="4dp"
        android:textSize="12sp"
        android:textColor="@color/text_secondary"
        app:layout_constraintEnd_toEndOf="@id/text_message_body_sent"
        app:layout_constraintStart_toStartOf="@id/text_message_body_sent"
        app:layout_constraintTop_toBottomOf="@id/text_message_body_sent" />
</androidx.constraintlayout.widget.ConstraintLayout>
